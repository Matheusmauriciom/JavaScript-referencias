// 1 - Encontrando um elemento específico:
//Dado um array de números, use o método find para encontrar o primeiro número que seja maior que 5.
//1 - Finding a specific element:
// Given an array of numbers, use the find method to find the first number that is greater than 5.

//2- Localizando um objeto em um array:
//Crie um array de objetos com informações de pessoas (por exemplo, nome e idade). Use o método find para encontrar a primeira pessoa que tenha mais de 30 anos.
//2- Finding an object in an array:
//Create an array of objects with people information (e.g. name and age). Use the find method to find the first person who is over 30 years old.

//3- Encontrando um livro em uma biblioteca:
//Crie um array de objetos representando livros em uma biblioteca (cada objeto pode ter propriedades como título, autor e ano de publicação). Use o método find para encontrar o primeiro livro escrito por um autor específico.
//3- Finding a book in a library:
//Create an array of objects representing books in a library (each object can have properties such as title, author and year of publication). Use the find method to find the first book written by a specific author

//4- Buscando um usuário pelo nome de usuário:
//Crie um array de objetos representando usuários de um site (cada objeto pode ter propriedades como nome, nome de usuário e email). Use o método find para localizar um usuário pelo nome de usuário.
//4- Searching for a user by username:
//Create an array of objects representing users of a website (each object can have properties such as name, username and email). Use the find method to find a user by their username

//5- Encontrando um item em estoque:
//Crie um array de objetos representando itens em estoque de uma loja (cada objeto pode ter propriedades como nome do item, preço e quantidade em estoque). Use o método find para encontrar o primeiro item que esteja em promoção (por exemplo, com preço reduzido).
//5- Finding an item in stock:
//Create an array of objects representing items in stock at a store (each object can have properties such as item name, price and quantity in stock). Use the find method to find the first item that is on sale (for example, reduced in price).

//6- Procurando uma palavra em um dicionário:
//Crie um array de objetos onde cada objeto representa uma palavra em um dicionário (por exemplo, palavra e definição). Use o método find para encontrar a definição de uma palavra específica.
//6- Looking for a word in a dictionary:
//Create an array of objects where each object represents a word in a dictionary (e.g. word and definition). Use the find method to find the definition of a specific word

//7 -Localizando um produto por código de barras:
//Crie um array de objetos representando produtos em um supermercado (cada objeto pode ter propriedades como nome, código de barras e preço). Use o método find para encontrar um produto pelo seu código de barras.
//7 -Locating a product by barcode:
//Create an array of objects representing products in a supermarket (each object can have properties such as name, barcode and price). Use the find method to find a product by its barcode.

//8- Encontrando um país por capital:
//Crie um array de objetos representando países e suas capitais (por exemplo, país e capital). Use o método find para localizar um país específico pela sua capital.
//8- Finding a country by capital:
//Create an array of objects representing countries and their capitals (e.g. country and capital). Use the find method to locate a specific country by its capital.

//9- Localizando um e-mail em uma lista de contatos:
//Crie um array de objetos representando contatos de email (cada objeto pode ter propriedades como nome e endereço de email). Use o método find para encontrar um contato pelo seu endereço de email.
//9- Finding an email in a contact list:
//Create an array of objects representing email contacts (each object can have properties such as name and email address). Use the find method to find a contact by their email address.

//10- Encontrando um evento em uma agenda:
//Crie um array de objetos representando eventos em uma agenda (cada objeto pode ter propriedades como data, horário e descrição). Use o método find para localizar o primeiro evento que ocorra em uma data específica.
//10- Finding an event in a calendar
//Create an array of objects representing events on a calendar (each object can have properties such as date, time and description). Use the find method to find the first event that occurs on a specific date.
