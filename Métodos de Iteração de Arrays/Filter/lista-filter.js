
// 1- Dado a array de números, faça a filtragem de números pares
// 1- Given an array of numbers, filter out even numbers.

// 1- Dado uma array de números use o método filter para retornar os números maiores que 20.
// 2- Given an array of numbers, use the filter method to return numbers greater than 20.

// 3- Dado a array de números, faça a filtragem de números impares.
// 3- Given an array of numbers, filter out odd numbers.

// 4- Crie uma função que retorne um array contendo apenas objetos de nomes com 7 letras ou mais."
// 4- Create a function that returns an array containing only object names with 7 letters or more."

// 5- Crie uma função que retorne um novo array contendo apenas objetos com as seguintes propriedades específicas: idade maior que 30 anos e sexo masculino.
// 5- Create a function that returns a new array containing only objects with the following specific properties: age greater than 30 and male gender."

// 6- Retorne uma array de pessoas,  cujo nome termina com a letra "A"; //endsWith
// 6- Return an array of people, whose name ends with the letter "A";

// 7- Filtrar números primos:
// Crie uma função que receba uma lista de números e retorne um novo array contendo apenas os números primos da lista.

// 7- Filter prime numbers:
// Create a function that takes a list of numbers and returns a new array containing only the prime numbers in the list.

// 8- Filtrar números com dígitos repetidos:
//Crie uma função que receba uma lista de números e retorne um novo array contendo apenas os números que não possuem dígitos repetidos (por exemplo, 1223 não seria incluído no resultado).

// 8- Filter numbers with repeating digits:
//Create a function that takes a list of numbers and returns a new array containing only numbers that do not have repeating digits (for example, 1223 would not be included in the result).

//9 - Filtrar números divisíveis por um valor específico:
//Crie uma função que receba uma lista de números e um número divisor. A função deve retornar um novo array contendo apenas os números da lista que são divisíveis pelo número divisor.

//9 - Filter numbers divisible by a specific value:
//Create a function that takes a list of numbers and a divisor number. The function must return a new array containing only the numbers in the list that are divisible by the divisor number

//10- Filtrar números entre um intervalo específico:
//Você deve criar uma função chamada filtrarNumerosNoIntervalo que recebe dois parâmetros:
//1 -Uma lista de números, representada por um array chamado numeros.
//20 Dois valores numéricos, min e max, que definem um intervalo.
//Sua função deve retornar um novo array contendo apenas os números da lista numeros que estão dentro do intervalo especificado (inclusive os limites min e max).

//10- Filter numbers between a specific range:
//You must create a function called filtrarNumerosNoIntervalo that takes two parameters:
//1 - A list of numbers, represented by an array called numbers.
//2- Two numeric values, min and max, that define a range.
//Your function must return a new array containing only the numbers from the list that are within the specified range (including the min and max limits).

//11 - Filtrar por gênero:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e gênero) e um gênero específico.
//A função deve retornar um novo array contendo apenas as pessoas do gênero especificado.

//11 - Filter by genre:
//Create a function that receives a list of objects representing people (with properties such as name and gender) and a specific gender.
//The function must return a new array containing only people of the specified gender.

//12 - Retornar a pessoa mais velha:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e idade) e retorne o objeto da pessoa mais velha.

//12 - Return the oldest person:
//Create a function that receives a list of objects representing people (with properties such as name and age) and returns the oldest person's object.

// 13- Crie uma função que retorne um novo array contendo apenas os objetos que têm uma propriedade específica, como a idade, com um valor correspondente.
// 13- Create a function that returns a new array containing only objects that have a specific property, such as age, with a corresponding value.

//14 - Filtrar objetos com propriedades ausentes:
//Crie uma função que retorne um novo array contendo apenas os objetos que têm uma determinada propriedade, como "curso", ausente ou nula.

//14 - Filter objects with missing properties:
//Create a function that returns a new array containing only objects that have a certain property, such as "course", missing or null.

//15 - Filtrar por cidade:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e cidade) e uma cidade específica. 
//A função deve retornar um novo array contendo apenas as pessoas que moram na cidade especificada.

//15 - Filter by city:
//Create a function that receives a list of objects representing people (with properties such as name and city) and a specific city.
//The function must return a new array containing only people who live in the specified city

//16 - Filtrar por idade mínima:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e idade) e uma idade mínima. 
//A função deve retornar um novo array contendo apenas as pessoas que têm idade igual ou superior à idade mínima especificada.

//16 - Filter by minimum age:
//Create a function that receives a list of objects representing people (with properties such as name and age) and a minimum age.
//The function must return a new array containing only people who are equal to or greater than the specified minimum age.

//17 - Retornar as pessoas com idade entre um intervalo específico:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e idade) e dois valores, min e max, que definem um intervalo de idade. A função deve retornar um novo array contendo apenas as pessoas cuja idade está dentro do intervalo especificado.

//17 - Return people aged between a specific range:
//Create a function that receives a list of objects representing people (with properties such as name and age) and two values, min and max, that define an age range. The function must return a new array containing only people whose ages fall within the specified range

//18 - Filtrar por país:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome e país) e um país específico. A função deve retornar um novo array contendo apenas as pessoas que são do país especificado.

//18 - Filter by country:
//Create a function that receives a list of objects representing people (with properties such as name and country) and a specific country. The function must return a new array containing only people who are from the specified country.

//19 - Filtrar por múltiplas propriedades:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome, idade e gênero) e um objeto de filtro. O objeto de filtro pode conter várias propriedades e valores que devem ser usados para filtrar as pessoas. A função deve retornar um novo array contendo apenas as pessoas que correspondem a todas as condições especificadas no objeto de filtro.

//19 - Filter by multiple properties:
//Create a function that receives a list of objects representing people (with properties such as name, age and gender) and a filter object. The filter object can contain various properties and values ​​that should be used to filter people. The function must return a new array containing only people who match all the conditions specified in the filter object.

//20 - Filtrar por nome parcial:
//Crie uma função que receba uma lista de objetos representando pessoas (com propriedades como nome) e uma string de busca. A função deve retornar um novo array contendo apenas as pessoas cujo nome contém a string de busca (ignorando maiúsculas/minúsculas).

//20 - Filter by partial name:
//Create a function that receives a list of objects representing people (with properties such as name) and a search string. The function must return a new array containing only the people whose name contains the search string (ignoring case)






