/* 
1- Remoção de Elementos de um Array:
Dado um array de números, use o método splice() para remover um elemento específico do array.
1- Removing Elements from an Array:
Given an array of numbers, use the splice() method to remove a specific element from the array.

2- Inserção de Elementos em um Array:
Crie um array vazio e use o método splice() para inserir um elemento em uma posição específica no array.
2- Inserting Elements into an Array:
Create an empty array and use the splice() method to insert an element at a specific position in the array.


3- Substituição de Elementos:
Dado um array de cores, use o método splice() para substituir uma cor por outra em uma posição específica.
3- Replacement of Elements:
Given an array of cores, use the splice() method to replace one color with another at a specific position.

4- Remoção de Múltiplos Elementos:
Dado um array de números, use o método splice() para remover vários elementos consecutivos do array.
4- Removal of Multiple Elements:
Given an array of numbers, use the splice() method to remove multiple consecutive elements from the array.

5- Inserção de Vários Elementos:
Crie um array vazio e use o método splice() para inserir vários elementos em uma posição específica no array.
5- Insertion of Various Elements:
Create an empty array and use the splice() method to insert multiple elements at a specific position in the array.

6- Cópia de Elementos:
Dado um array de strings, use o método splice() para copiar um subconjunto de elementos em um novo array.
6- Copy of Elements:
Given an array of strings, use the splice() method to copy a subset of elements into a new array.

7- Corte de Elementos:
Dado um array de palavras, use o método splice() para cortar uma porção do array, mantendo apenas a parte que você deseja.
7- Element Cutting:
Given an array of words, use the splice() method to cut a portion of the array, keeping only the part you want.

8- Inserção de Elementos no Início:
Dado um array de números, use o método splice() para inserir elementos no início do array.
8- Inserting Elements at the Start:
Given an array of numbers, use the splice() method to insert elements at the beginning of the array.

9- Remoção de Elementos no Final:
Dado um array de nomes, use o método splice() para remover elementos do final do array até que ele atinja um tamanho específico.
9- Removing Elements at the End:
Given an array of names, use the splice() method to remove elements from the end of the array until it reaches a specific size.


10 Inserção de Elementos no Meio:
Dado um array de letras, use o método splice() para inserir elementos no meio do array, deslocando os elementos existentes para acomodar os novos. 
10 Insertion of Elements in the Middle:
Given an array of letters, use the splice() method to insert elements into the middle of the array, shifting existing elements to accommodate new ones. 
*/